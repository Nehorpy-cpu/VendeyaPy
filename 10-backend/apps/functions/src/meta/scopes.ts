/**
 * meta/scopes.ts — Permisos (scopes) requeridos para WhatsApp por tenant (Fase 4B).
 */
export const META_REQUIRED_SCOPES = ['whatsapp_business_messaging', 'whatsapp_business_management'] as const;
