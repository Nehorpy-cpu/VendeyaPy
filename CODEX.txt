## Resumen para continuar en otra computadora

**Proyecto:** VendeYaPy, plataforma SaaS multi-tenant para automatizar ventas por WhatsApp en Paraguay.

**Repositorio:** GitHub `Nehorpy-cpu/VendeyaPy`, monorepo ubicado actualmente en `C:\AI_AFG\10-backend`.

**Stack:** Next.js, TypeScript, Firebase Auth/Firestore/Storage/Cloud Functions, WhatsApp Cloud API, Anthropic Claude y pnpm workspaces.

### Estado actual

- Producción: `vpw-prod-dd6ff`
- Panel: `https://vendeyapy.com` (dominio productivo desde 2026-07-13) y `https://vpw-prod-dd6ff.web.app`
- Tenant real: `arfagi`
- WhatsApp real conectado y en modo `live`.
- Webhook Meta operativo y firmado.
- Token de WhatsApp cifrado por tenant.
- Arquitectura multi-número implementada.
- Handoff humano implementado: el vendedor puede tomar una conversación, responder desde el panel y devolverla al bot.
- Flujo completo probado: consulta → recomendación → carrito → checkout → comprobante → verificación → pedido.
- Pedidos protegidos por callables, reglas, auditoría y estados permanentes.
- Comprobantes recibidos por WhatsApp se descargan y guardan en Firebase Storage.
- Agente F1–F6 desplegado: contexto, nombres de productos, confirmaciones naturales, anti-alucinaciones, checkout idempotente y bienvenida con intención.
- Catálogo CAT-1/CAT-2/CAT-2B desplegado: ficha estructurada para IA, ranking por ocasión, respuestas honestas y alternativas determinísticas.
- OCV-1 (ORDER-COMPROBANTE-VIEW-1) completado: visor de comprobantes en el panel con URLs temporales firmadas; el bucket sigue privado.
- Migración de dominio completada (2026-07-13): `vendeyapy.com` sirve producción con SSL y ownership activos; `www.vendeyapy.com` redirige 301 al apex (restaurado vía `customDomains.undelete` tras un borrado accidental). Sin deploy ni cambios de DNS adicionales.
- WHATSAPP-AGENT-F7 desplegado y cerrado técnicamente (2026-07-15): fidelidad estricta en consultas por producto/marca (commit `97bb035`), deploy con selector mínimo de 4 funciones y `.env` sin cambios. Smoke live final aprobado: consulta estricta devuelve solo coincidencias reales, negación honesta de pertenencia, similitud con alternativas etiquetadas, marcas correctas, cero mutaciones comerciales. Observación de baja prioridad: desliz de marca de la IA en comparaciones observado en el primer smoke (no se reprodujo en el final).
- Incidente de cupo de IA (2026-07-15, mitigado): 251.398 tokens usados vs límite anterior de 250.000; 65 llamadas 100% atribuidas (WhatsApp real del owner), sin doble conteo, consumo dominado por input/contexto; sin IA el bot degradaba a reglas determinísticas en silencio (mitigado el 2026-07-16 por AI-FALLBACK-HONESTO-1: ahora deriva a un humano con aviso). Límite growth actualizado 250.000 → 1.500.000 (contador y período preservados), servicio restaurado y verificado. Próximo reset: 01-ago-2026.
- HANDOFF-2 desplegado y cerrado de punta a punta (commit `e0c284e`, 2026-07-15): el cliente pide una persona (genérico, por nombre configurado o "nuevamente") y el sistema ejecuta el pase REAL antes de la IA — detección determinística con negaciones ignoradas, servicio canónico executeHandoff (transaccional, idempotente, razones estructuradas), notificación única por wamid, prompt guard anti-promesas, bot silencioso durante takeover, liberación manual con metadata limpia y retorno del bot. Comprobante/pago manual preservados. Deploy: 9 funciones (selector auditado) + Hosting (CTA "Ver conversación"); Rules sin cambios; cero deletes. Smokes productivos aprobados, incluido el de silencio (mensaje durante takeover persistido sin respuesta del bot ni IA) y cero mutaciones comerciales.
- AI-FALLBACK-HONESTO-1 desplegado y cerrado de punta a punta (commit `855b00d`, 2026-07-16): cuando una consulta necesita IA y el cupo está agotado, el bot ya no degrada en silencio — deriva a un humano con honestidad. Estados de bloqueo estructurados por código (`quota_exhausted`/`feature_unavailable`/`configuration_error`/`provider_transient_error`/`empty_reply`); SOLO `quota_exhausted` activa el pase (y solo si la consulta es derivable: la cortesía pura no deriva); razón canónica `ai_unavailable` vía executeHandoff; vendedor resuelto desde la config del tenant; persistencia ANTES de confirmar el pase; notificación de campana idempotente (bucket diario si no hay vendedor); silencio si ya estaba tomado; simuladores sin efectos operativos para este fallback. Deploy: selector corregido a 9 funciones (la propuesta inicial era de 4; el grafo de imports mostró que handoff.ts también alcanza a las funciones de takeover/comprobante) + Hosting; cero Rules/deletes/recreates; `.env`, registro cerrado y config productiva preservados. Verificación honesta: la lógica de cuota se validó en tests/emulador (unit 34/34, E2E 9/9 + regresiones en verde) — el agotamiento NO se forzó en producción; prod pasó el smoke de no-regresión con IA disponible (respuesta real de la IA, "Gracias" sin handoff, contador = suma exacta de aiRequests, cero handoffs/notificaciones nuevas, carrito/pedidos/pagos intactos).
- COVERAGE-GUARD-1 desplegado y cerrado de punta a punta (commit `1497b40`, 2026-07-16): interceptor determinístico ANTES de la IA para consultas de cobertura/zona/costo/plazo de envío → respuesta segura fija que NO afirma cobertura e indica que el equipo confirma según ubicación (pase a humano solo si el cliente lo pide, vía HANDOFF-2). Regla secundaria en el prompt (jamás extrapolar la FAQ de envíos ni inventar costo/plazo). Exclusiones del interceptor: checkout/pago gana siempre, tracking de pedido existente, comprobantes, pedir imágenes/QR/link, retiro en local (durante takeover el bot ya estaba mudo por HANDOFF-2, aguas arriba del guard). Deploy: selector mínimo de 4 funciones (onWebhookInbox, simulateAgentMessage, agentTestCaseRun, devMessage); cero Hosting/Rules/indexes/Storage; cero deletes/recreates; `.env` y registro cerrado preservados. Smoke productivo aprobado: consultas por el interior y por Encarnación → respuesta segura exacta, cero llamadas y tokens de IA, cero handoffs/notificaciones, carrito/órdenes/pagos intactos, ningún dato bancario, envío live y logs limpios. Es la mitigación inmediata mientras se construye COVERAGE-1 (la captura de ubicación y la aprobación de cobertura NO existen todavía).
- [HISTÓRICO — superado por la reactivación del 2026-07-25] COVERAGE (1B/1C/1D + hardening + kill-switch atómico + ubicación-OFF-inerte) desplegado entonces con el flag apagado (commit `0326784`, 2026-07-18): gate de cobertura con aprobación manual — captura de ubicación nativa/dirección escrita (la ubicación exacta solo en `coverageRequests`, jamás a logs/IA/notificaciones), revisión humana en el panel (aprobar/rechazar/solicitar info; OWNER/MANAGER, SELLER solo asignado, PLATFORM_ADMIN solo lectura), reanudación idempotente con orden y banco SOLO después de aprobación, comprobante/pago manual preservados, expiración + purga de ubicación a 30 días, contrato fail-closed `enabled + activationId` (el flag se re-lee dentro de cada transacción y antes de cada envío; activaciones antiguas quedan inertes). Deploy sin `--force`: índice 13→14 (exactamente 1 CREATE, cero DELETE), 15 Functions actualizadas + 7 creadas (onWebhookInbox primero), Rules y Hosting, cero deletes/recreates, IAM/Eventarc/Scheduler automáticos, `.env` intacto, registro cerrado, WhatsApp live. Cobertura sigue AUSENTE/OFF para arfagi y credipower (cero activationId, cero requests/jobs/outbox/notificaciones). Smoke flag-OFF aprobado (2026-07-18): saludo normal; consulta de envíos interceptada por Coverage Guard con la frase segura (cero IA para el tester); cero Coverage requests/jobs/outbox/notificaciones; sesión del tester IDLE; órdenes intactas; pedido PENDING_VERIFICATION intacto sin auto-pago. El delta de cupo IA observado fue tráfico live concurrente de OTRO cliente (gap conocido de atribución `customerId:null`), no del tester ni del deploy. ESTADO: infraestructura Coverage EN PRODUCCIÓN; Coverage para clientes APAGADO; NO funcionalmente completo hasta activar (activationId nuevo) y aprobar el E2E real. Deuda no bloqueante: NEXT_PUBLIC_META_CONFIG_ID y NEXT_PUBLIC_SUPPORT_WHATSAPP sin configurar en el build de hosting (verificar las vars públicas de Meta antes de Meta Catalog Live/onboarding); warnings preexistentes de runtime Node 20 (decomisiona 2026-10-30) y firebase-functions 4.9.0; AI-USAGE-ATTRIBUTION-1 pendiente.
- **[HISTÓRICO — el estado flag-OFF fue superado por la reactivación del 2026-07-25] SHIPPING-CHAT (paquete completo: parser determinístico compartido + preview 2B + saga backend 3C + HARDEN-1/2/3/4 + integración panel 4B + HARDEN-1/2 web) — DESPLEGADO EN PRODUCCIÓN (entonces con Coverage apagado)** (commit `5f8ccbc`, deploy 2026-07-24): cotización de envío confirmada desde la revisión de cobertura (ADR-0011) — la IA jamás decide dinero; preview con confirmación explícita; saga TX-A→claim→Meta→TX-C con el outbox como única fuente de verdad; recuperación durable (pointer + fase derivada) y reconciliación humana de envíos `unknown`; sweep anti-inanición; **HARDEN-4 cerró el `sending` varado por crash duro (el sweep lo normaliza a unknown con campana idempotente) y los workers zombis (settlement con ownership por generación `attempts`: un zombi jamás pisa estado ni produce un segundo envío físico)**. Integración completa en /conversations: preview, chips de fase, resolución manual, gate del composer espejo del server, aislamiento por intento/conversación y evidencia financiera congelada. **Auditorías 4C + delta aprobadas sin CRÍTICO/ALTO/MEDIO.** **Deploy 2026-07-24** (sin `--force`): índice 14→15 (exactamente **1 CREATE** `coverageMessageOutbox action/status/stuckNotifiedAt/updatedAt`, **cero DELETE**) → 15/15 READY; **15 Functions UPDATE + 3 CREATE** (`coverageQuoteAndApprove`, `coverageQuoteResolveUnknown`, `coverageQuoteAttemptState`), consumidores (`onWebhookInbox`, `onCoverageResumeJob`) PRIMERO; **92/92 Functions ACTIVE**; cero deletes/recreates; **Hosting desplegado** (UI de cotización verificada en el chunk de conversaciones; registro sigue "por invitación"); **Rules NO desplegadas** (sin cambios); hash de `.env.vpw-prod-dd6ff` preservado pre/post; registro cerrado; WhatsApp `live`. **Smoke flag-OFF aprobado (2026-07-24)**: saludo normal determinístico; consulta de envíos respondida por el Coverage Guard con la frase segura (no promete cobertura/costo/plazo); **cero IA** (tokens idénticos); **cero documentos Coverage**; cero pedidos/pagos/notificaciones nuevos; carrito del tester intacto; logs sin errores. **Estado real: infraestructura Shipping Chat EN PRODUCCIÓN; Coverage AUSENTE/OFF en arfagi y credipower (cero activationId); la funcionalidad completa AÚN NO está validada con Coverage activo.** La activación y el E2E se completaron el 2026-07-25 (ver bullet GO-LIVE + PURGE-FIX-1). credipower sigue diferido e intocable. FASE 3 (número externo) sigue pendiente — este smoke del owner no la completa. Meta Catalog Live sigue en backlog posterior.
- **COVERAGE GO-LIVE + PURGE-FIX-1 — E2E REAL VALIDADO Y COVERAGE REACTIVADO SOLO PARA ARFAGI** (fix `eb28365`, deploy y E2E 2026-07-25): **el E2E de Coverage/Shipping Chat — hasta comprobante y cancelación manual, sin venta pagada — quedó VALIDADO en producción** con el tester interno reutilizado (número del owner, no publicado): checkout gateado (el pedido CANCELLED previo no se reutilizó) → ubicación (privacidad verificada: coordenadas SOLO en el request, placeholder redactado, campana sin PII) → rechazo con nota interna jamás filtrada → reintento con solicitud nueva → cotización ₲30.000 (renovación segura de huella en el primer intento) → mensaje canónico ÚNICO con wamid real → aprobación → **pedido único ₲280.000** (envío separado del subtotal; la ganancia jamás incluye el envío) → instrucciones bancarias con el total con envío UNA vez → comprobante a Storage con derivación al vendedor → **pedido de prueba cancelado manualmente sin confirmar el pago (no hubo venta pagada; cero PAID en todo el E2E; cero IA)**. **FASE 3 con número externo sigue PENDIENTE — este E2E no la completa.** **Gap de privacidad corregido (PURGE-FIX-1)**: las decisiones terminales (aprobación y rechazo, incluida la aprobación autoritativa de la saga de cotización) ahora programan la purga a 30 días (elimina coordenadas exactas y nombre del lugar; la dirección textual NO la elimina este maintenance) EN LA MISMA transacción — antes solo expiración/cancelación lo hacían; los reintentos idempotentes jamás reinician la fecha; una dirección textual (sin coordenadas) queda sin purga por diseño. Verificación: batería completa en verde + E2E review 36/36 · resume 28/28 · killswitch 23/23 · saga 81/81 (con checks nuevos de purga) + 2 revisores adversariales APROBADO. **Deploy**: selector mínimo de 4 callables (`coverageApprove`, `coverageReject`, `coverageQuoteAndApprove`, `coverageQuoteResolveUnknown`) — 4 updates / 0 create / 0 delete; cero Hosting/Rules/índices (las 3 funciones read-only del mismo archivo quedaron excluidas a propósito: comportamiento idéntico). **Backfill**: los 2 requests históricos del E2E (coordenadas presentes, purga ausente) recibieron `coordinatesPurgeAt = decisión + 30 días` con updateMask y precondición por documento (gate "exactamente 2" verificado). **Coverage REACTIVADO exclusivamente en arfagi** con un activationId NUEVO y opaco (el anterior queda como histórico y JAMÁS se reutiliza), política `required` con `maxChargeGs ₲200.000` y expiración 24 h; **credipower sigue apagado e intocable**. Kill-switch vigente: apagar = mutación única de `coverage.enabled=false` releyendo un updateTime fresco.
- Gap conocido SIN impacto actual: la campana de notificaciones solo la ve TENANT_OWNER por rules (salvo `catalog_quality`, que desde el 2026-07-29 también ve TENANT_MANAGER) (el vendedor configurado actual ES el owner); un futuro usuario con rol SELLER puro vería la bandeja de conversaciones pero no la campana — programa posterior.
- No hay pedidos pendientes de verificación: los dos pedidos de prueba (el de 2026-07-13 y el del E2E del 2026-07-25) fueron cancelados manualmente sin confirmar el pago. De los 15 pedidos históricos, 14 están CANCELLED y 1 quedó en estado PAID (anterior a los releases de julio; no lo originó ninguna fase reciente).
- **META CATALOG — sync real + reconciliación + importación (estado vigente 2026-07-30)** (commits `1d2bad5` y `141c4ab`, ambos desplegados): acceso a Meta desbloqueado con token permanente de system user con `catalog_management` (`business_management` NO está y NO es bloqueante: solo limita el descubrimiento a nivel Business). Catálogo productivo `Arfagi_Catalogo_Perfumes` (id 1032991642570797) vinculado al WABA. Config de arfagi en `mode:dry_run`; credipower sin config. **Contrato fail-closed**: solo sincronizan los productos con `syncToMeta===true` y `stockPendingReview!==true` — el resto queda fuera por completo (sin create, update ni **disable**), que es lo que impide apagar los artículos vivos del negocio al importar. `Product.metaRetailerId` es la identidad remota, separada del SKU interno (que nunca se altera) y del `metaProductItemId`; unicidad por lock transaccional. **Mapping confirmado**: Armaf Odyssey Mega ↔ ARM-744646-5202, hoy con `syncToMeta=true` (único opt-in tras el canary); **el precio autoritativo local es ₲250.000**; el canary del 2026-07-28 lo llevó a Meta pero **el feed diario del sitio lo revirtió a ₲130.000 el 2026-07-30** (ver CONFLICTO DE FUENTE DE VERDAD abajo). Supremacy Not Only Intense NO es el Collector's Edition de Meta: sigue sin vínculo, candidato a crearse. **Importación inicial: 30 artículos** (solo los limpios y con categoría inequívoca), todos INACTIVE + `syncToMeta=false` + `stockPendingReview=true`, **sin productFinancials (costo desconocido, no 0)**, sin stock inventado, id determinístico `meta_<retailerId>`, invisibles para el bot y el checkout. **Pendientes de revisión que quedaron entonces (102, cifra HISTÓRICA — la importación genérica del 2026-07-30 los trajo a todos como borradores INACTIVE)**: 67 nombres genéricos, 13 duplicados probables, 26 incoherencias nombre/descripción, 1 sin marca, 1 sin stock, 1 sin categoría inequívoca, 1 excluido por decisión; el conteo VIGENTE de calidad es 23 genéricos, 18 duplicados probables, 33 incoherencias, 1 sin marca. **UNA escritura nuestra en Meta en toda la historia: el canary de Odyssey (2026-07-28, aprobado por el owner) — UPDATE exclusivamente de `price` ₲130.000→₲250.000 sobre `ARM-744646-5202`; preview consumido UNA vez, 1 job `succeeded` en 1 intento, confirmación releyendo Meta, 1 sync log, cero incidencias; nombre/descripción/imagen/availability/url del item intactos; 181 artículos antes y después; los otros 32 productos sin opt-in. Estado vigente: `mode=dry_run` de vuelta (mutación única con precondición), Odyssey mantiene `syncToMeta=true` y su `synced` LOCAL (desactualizado desde el 30-jul por la reversión del feed); una nueva aplicación real requeriría volver a `live` con aprobación del owner; desde el 2026-07-31 además sería INERTE, porque con `external_managed` y cero campos propios no existe patch posible (ese `synced` local quedó superado: hoy Odyssey figura `drifted_external`).** El catálogo de Meta creció EXTERNAMENTE de 133 a **181** artículos entre el 27 y el 28 de julio (49 altas + 1 baja vía Commerce Manager, verificado con conteo directo y diff item por item — sin defecto de conteo). Falta enriquecer los importados (costo y stock reales) antes de habilitar cualquier sincronización.
- **META CATALOG — outbox de escrituras + preview binding, DESPLEGADOS 2026-07-28 (ADR-0012/0013)**: el contrato de escritura de `items_batch` fue corregido contra la doc oficial (`d488c39`+`da538f8`: `title`/`image`/`link` en vez de los nombres de lectura, identidad en `data.id` sin truncar, `allow_upsert:false`, CREATE/UPDATE/DISABLE separados, validador también en el cliente real) y `catalogSyncApply` ya NO escribe en Meta: encola jobs persistidos (`9495602`+`4ea0640`+HARDEN-2) con intención deduplicada solo sobre trabajo ACTIVO, ciclos inmutables con log atómico por ciclo, claim/lease/fencing, verificación de tres estados contra el catálogo real, CAS en toda acción humana (revisar/descartar desde el panel, jamás reenviar a ciegas), tope de consultas por intentos y bandeja de incidencias por campo persistido `attentionRequired`. **DESPLEGADO el 2026-07-28 (`50793bc`, DEPLOY-META-CATALOG-OUTBOX-DRY-RUN-1): 96→100 Functions ACTIVE, 2 índices nuevos READY, rules cerradas, `devRunMetaCatalogOutbox` AUSENTE de producción, scheduler cada 5 min INERTE con `dry_run` (corridas 200 en silencio: cero claims, cero POST), hosting con la bandeja de incidencias; smoke humano aprobado.** Canary Odyssey PREPARADO (simulación con código real: exactamente 1 update; curar la ficha local ANTES — el apply pisaría nombre/descripción/imagen/precio remotos). **META-CATALOG-PREVIEW-BINDING-1 (`fbb7aab`) DESPLEGADO 2026-07-28 + SMOKE APROBADO**: el apply exige `{previewRunId, planHash}` del dry_run aprobado (TTL 10 min, mismo actor, mismo catálogo), replanifica y solo encola con coincidencia EXACTA; consumo transaccional anti-replay; discrepancia ⇒ `failed-precondition` con outbox 0/0. Panel: "Previsualizar cambios" / "Enviar 1 cambio a Meta (Producto)". Deploy: 3 Functions UPDATE (`runTenantJob`, `simulateAgentMessage`, `devSyncCatalogToMeta`) + Hosting; cero CREATE/DELETE/rules/índices. Smoke productivo: corrida dry_run/planned con planHash persistido y NO consumido, Crear/Actualizar/Deshabilitar/Bloqueados = 0, 33 sin opt-in, remoteOnly=150; cero applies, cero outbox, cero items_batch; Meta seguía en 181 con Odyssey en ₲250.000 al 2026-07-29 (revertido a ₲130.000 por el feed del sitio el 30-jul); config sigue dry_run. El canary controlado de Odyssey se auditó y EJECUTÓ el mismo 2026-07-28 con éxito (ver el cierre en el bullet anterior). **ONBOARDING GENÉRICO + CALIDAD (META-CATALOG-GENERIC-ONBOARDING-QUALITY-1, ADR-0014 — DESPLEGADO 2026-07-29)**: importación paginada REANUDABLE de catálogos de cualquier tamaño/vertical (runs persistidos con cursor por página, lease anti-concurrencia, idempotencia por ítem, producto+lock en la misma tx, categoría desconocida ⇒ borrador «sin clasificar», ambiguo ⇒ humano), evaluador de calidad server-side (BLOCKING de los mismos gates existentes + WARNINGs; fingerprints con resolvedAt; marca neutral `Product.brand`; perfumería ya no es requisito universal), centro de calidad en el panel (summary con truncated honesto, campana agregada idempotente con autocierre, editor por secciones, selección masiva solo de elegibles), fix de vendibilidad en searchCatalog. **HARDEN-1 (mismo ADR-0014): default multi-vertical REAL (sin perfil ⇒ generic, sin ficha perfume, marca solo en Product.brand; perfumeria SOLO por config explícita — GATE del release: escribir config/catalog.profile={vertical:'perfumeria'} en arfagi ANTES de desplegar las Functions), fencing generacional del run de import (worker vencido ⇒ claim_lost sin escribir NADA; cursor/contadores monotónicos), locks también en el camino legacy (importItems + confirmMapping transaccional), operación de mantenimiento metaCatalogMaintenanceRun preview/apply para backfill de locks y quality (conflictos SIN ganador automático; EJECUTADO el 2026-07-29: preview + apply con run NUEVO, jamás el del preview), campana catalog_quality visible para manager (queries de igualdad calcadas de rules).** Verificación previa al deploy: batería 1236+213+245 en verde, E2E onboarding 119/119, regresión verify-meta-catalog 194/194 ×2 (en ese momento, cero cambios en producción/Meta). **DESPLEGADO el 2026-07-29 (`6cfc464`, DEPLOY-META-CATALOG-ONBOARDING-QUALITY-DRY-RUN-1) + MANTENIMIENTO Y SMOKE APROBADOS**: perfil explícito `config/catalog.profile.vertical='perfumeria'` de arfagi (escritura única con updateMask anidado; sin esto el default nuevo lo dejaba en `generic`) → **1 índice compuesto nuevo** `products(status, quality.blocking)` (17→18 READY; el "cero índices" de reportes previos era erróneo) → 3 rules cerradas + campana `catalog_quality` para manager (probes sin auth y cross-tenant: 403) → **5 functions CREATE + 21 UPDATE** (100→105 ACTIVE, cero delete/recreate, sin --force, `devRunMetaCatalogOutbox` ausente, hash `.env` intacto, gate app-level `UNAUTHENTICATED` verificado) → hosting. **Mantenimiento**: preview sin escrituras de negocio y apply autorizado con **30 locks creados + 1 preservado = 31, 32 quality, 1 archivado omitido, 0 conflictos, 0 errores**; campos comerciales/mappings/opt-ins byte a byte idénticos; 1 sola notificación agregada (31 con bloqueos: `not_active` ×30, `stock_pending_review` ×30, `product_url_missing` ×1). Cuentas técnicas efímeras autorizadas, eliminadas y verificadas. **Invariantes: dry_run intacto, cero imports/applies/jobs nuevos/items_batch, Meta en 181 y Odyssey en ₲250.000, credipower intacto, 0 errores.** La importación genérica quedó disponible pero NO se ejecutó ese día. **IMPORTACIÓN GENÉRICA EJECUTADA el 2026-07-30 (META-CATALOG-GENERIC-IMPORT-ARFAGI-1, smoke aprobado)**: 150 artículos que existían solo en Meta importados en UN import run `completed` (3 páginas, 181 procesados, 0 conflictos, 0 omitidos, 0 reinicios de cursor); 33→183 productos, 31→181 locks, 32→182 quality, remoteOnly 150→0; todos INACTIVE, syncToMeta=false, stock pendiente, sin costo, sin categoría (neutral), invisibles para bot/carrito/checkout, con quality al nacer; calidad: 2 limpios, 180 con bloqueos, 1 archivado sin evaluar, 18 duplicados probables señalados SIN fusionar; los 3 casos con exclusión humana previa se importaron seguros y quedan pendientes de revisión. Cero escrituras a Meta, cero outbox/items_batch, dry_run intacto, credipower intacto, cuenta efímera eliminada. **[HISTÓRICO — RESUELTO el 2026-07-31] CONFLICTO DE FUENTE DE VERDAD (2026-07-30)**: el catálogo tiene un FEED DIARIO propio de arfagi (03:33 America/Asuncion, CSV desde api.arfagi.com) que publica los 181 artículos y **revirtió el canary de Odyssey de ₲250.000 a ₲130.000**; el canary funcionó pero fue sobrescrito ~36 h después. `sourceOfTruth:"vendeyapy"` NO refleja la autoridad efectiva; Odyssey figura local como `synced` pese a la deriva (la detección de deriva no cubre los vinculados manualmente); el feed no se tocó. **RESUELTO el 2026-07-31**: con arfagi en `external_managed` y cero campos escribibles, las escrituras a Meta quedan bloqueadas POR CONSTRUCCIÓN, y la reconciliación cubre también los vinculados a mano (Odyssey quedó `drifted_external`, ya no `synced` a ciegas).
- **META CATALOG — AUDITORÍA DE FUENTE DE VERDAD COMPLETA + MODELO DE PROPIEDAD DESPLEGADO Y ARFAGI EN external_managed (auditoría 2026-07-30, release 2026-07-31, ADR-0015)**. La auditoría read-only (`META-CATALOG-SOURCE-OF-TRUTH-AUDIT-1`, cero escrituras) probó que el catálogo de Meta lo gobierna **al 100 % un feed diario del propio sitio del tenant** (`primary_feed` id `1668013347585391`, `deletion_enabled:true`, Server Fetch DIARIO 03:33 America/Asuncion — arranque real 03:23-03:24 — desde `api.arfagi.com`; **el token viaja en el query string y jamás se imprime, loguea ni persiste**), que publica los 181 artículos; `sourceOfTruth:"vendeyapy"` era una declaración sin efecto. **Decisiones del owner**: arfagi adopta `external_managed`; el sitio gobierna todos los campos públicos que publica; el precio correcto de Odyssey es ₲250.000 y **se corrige EN EL ORIGEN, no en Meta**; mientras el feed publique ₲130.000, Odyssey queda `drifted_external` y **no cierra venta automática**; el feed no se modifica ni se apaga; Meta sigue en `dry_run`. **Implementado (`META-CATALOG-OWNERSHIP-MODEL-1`)**: propiedad POR CAMPOS con tres modelos (`vendeyapy_managed`/`external_managed`/`hybrid`) y fail-closed en dos niveles (config ausente o contradictoria ⇒ cero campos escribibles + techo `dry_run`); detección de fuentes externas con metadata SANEADA que corre en onboarding, antes de autorizar `live`, periódicamente y justo antes de cada POST sensible — fuente nueva o huella cambiada BLOQUEA la escritura y levanta campana idempotente, sin tocar el feed; estado ACTUAL honesto (`verified`/`drifted`/`drifted_external`/`remote_missing`/`unverifiable`, con `stale` derivado en lectura por TTL) separado del histórico inmutable del job — `metaVerifiedAt` = última LECTURA, `metaLastSyncAt` = última ESCRITURA confirmada; reconciliación periódica paginada e idempotente que cubre importados **y vinculados a mano** (el agujero por el que Odyssey figuraba `synced` estando derivado) y que JAMÁS escribe en Meta, con scheduler 04:30 y 16:30 Asunción contra TTL de 24 h (dos oportunidades por ventana: una corrida perdida no apaga la venta); guard determinístico de deriva en bot y checkout (los productos cuyo precio no podemos afirmar no se listan, y si el cliente los nombra se deriva a una persona); migración preview/apply de la propiedad. **Consecuencia operativa de la migración**: los productos mapeados nacen `unverifiable` y no cierran venta hasta la primera reconciliación; los productos sin identidad remota no se ven afectados. Para arfagi eso es exactamente lo pedido (180 importados INACTIVE + Odyssey bloqueado). **DESPLEGADO Y MIGRADO EL 2026-07-31** (`RELEASE-META-CATALOG-OWNERSHIP-EXTERNAL-MANAGED-1`, commit **`d542cda`**, smoke humano aprobado; precedido de 6 auditores read-only y preflight completo: 458 checks focalizados, 2.297 tests, E2E 145/119/194 desde emulador limpio). **Superficie**: rules → functions → hosting; **6 CREATE + 39 UPDATE**, 0 delete/recreate, **sin `--force`**, **105 → 111 Functions ACTIVE**, **0 índices** (18 READY), **3 matches de Rules cerrados** (`metaCatalogSourceChecks`, `metaCatalogVerificationRuns`, `metaCatalogOwnershipRuns`; probes sin auth = 403), hash de `.env` idéntico, `devRunMetaCatalogOutbox` ausente, las 5 callables rechazan con UNAUTHENTICATED y el scheduler quedó privado con cron real `30 4,16 * * *` America/Asuncion. **Migración de arfagi** (cuenta técnica efímera, eliminada y verificada por lookup en cada corrida): `external_managed`, `ownedFields` vacío, los 10 campos públicos externos, `mode` sigue `dry_run`, **una sola fuente reconocida** (feed `1668013347585391`, host `api.arfagi.com`, DAILY 03:33 — metadata saneada, sin URL/query string/token), legacy `sourceOfTruth` preservado sin efecto, y la propiedad efectiva verificada **NO degradada** (si degradara, el guard quedaría apagado en silencio). **Reconciliación inicial** en la misma ventana: 181 remotos, 181 matched, 0 errores, detección de bajas ejecutada, TTL 24 h ⇒ **50 `verified` · 131 `drifted_external`** (campos: `availability` ×130 —los importados están INACTIVE y el feed los publica disponibles—, `brand` ×1, `title` ×1, **`price` ×1 = Odyssey local ₲250.000 vs publicado ₲130.000**), con el histórico `synced`/`metaLastSyncAt` preservado y separado del actual. **Invariantes**: huella comercial de los 183 productos idéntica al baseline (recalculada post-apply y post-reconciliación), mappings y locks idénticos, pedidos 15 y pagos 0 sin cambios, outbox con su único job histórico, **cero `items_batch` y cero escrituras a Meta**, credipower intacto, logs sin errores; de los 5 documentos de config del tenant el release tocó **exactamente uno** (`config/meta`). **Smoke humano**: panel con "Administrado por fuente externa", Odyssey ₲250.000 vs ₲130.000, sincronización bloqueada y sin URL/token a la vista; por WhatsApp **1 inbound y 2 salientes** — no afirmó el precio, no creó pedido, no mandó banco, derivó una sola vez con mensaje honesto, preservó el carrito, chat devuelto al bot con metadata limpia, **cero IA** y **una sola** notificación nueva (`handoff_catalog_drift`). **Dos defectos del runbook detectados y corregidos**: `--only functions` sin selector habría creado `devRunMetaCatalogOutbox` en producción, y el `.env.production.local` omitía `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN` (habría caído al valor DEMO por precedencia de Next, rompiendo reset de contraseña sin que un smoke de /login lo detectara). **Limitación conocida**: la tarjeta de propiedad muestra la fuente sin nombre, sin horario y SIN el aviso de que el feed borra de Meta los artículos que no publica (deletion_enabled:true) — dato operativamente relevante. **PENDIENTE (prioridad 0, fuera de este repo)**: corregir el precio de Odyssey **en el origen que genera el feed**; el feed sigue intacto, `mode` sigue `dry_run` y Meta sigue publicando ₲130.000.
- **ADJUNTOS DE CONVERSACION (imagenes y PDF) — EN REPO, NO DESPLEGADO (2026-07-31, ADR-0016, WHATSAPP-MEDIA-SAFE-FOUNDATION-1)**. La auditoria previa (read-only) probo tres defectos criticos: (1) TODA imagen entrante se trataba como comprobante — el mensaje nunca llegaba al bot, asi que la foto de un producto congelaba el pedido en PENDING_VERIFICATION, disparaba handoff y sacaba al bot del chat; (2) una imagen sin pedido pendiente ni se descargaba (se perdia: la unica ruta de Storage exigia orderId); (3) los PDF se descartaban en silencio, y las apps bancarias mandan los comprobantes como PDF. Ademas el modelo Message no tenia campo de adjunto (la evidencia era texto libre falsificable a mano), el MIME declarado por Graph se creia sin verificar, y el pendingOrderId de la sesion no se validaba contra el cliente que enviaba. IMPLEMENTADO: adjunto generico tenants/{t}/attachments/{id} con identidad determinista y create() (un reintento del webhook no duplica); DOS ejes ortogonales (ingesta received-downloading-verifying-stored con terminales, y clasificacion unclassified-generic_media|payment_receipt_candidate-payment_receipt_linked); descarga endurecida con timeout, reintentos solo de lecturas, corte por bytes DURANTE el stream, checksum y sniffing de magic bytes contra el declarado; ruta privada sin PII; emisor de URLs con TTL <=10 min que acepta las DOS familias (adjuntos nuevos y comprobantes legacy, que siguen abriendo); rules cerradas al cliente; retencion con purga APAGADA por defecto que nunca toca evidencia de pago. REGLA DE NEGOCIO: la ingesta JAMAS mueve un pedido; con contexto de pago declarado, un unico pedido admisible del mismo customerId y cero ambiguedad, el adjunto se PROPONE como candidato y se avisa por la campana, pero el pedido no se mueve; solo OWNER/MANAGER/SELLER lo vinculan, y ni eso confirma el pago (sigue siendo el callable de ORDER-1). Una imagen fuera del contexto explicito de pago queda como medio normal aunque parezca un comprobante. CAMBIO OPERATIVO: antes cualquier imagen movia el pedido y derivaba automaticamente; ahora la decision es del vendedor. VISION, OCR y reconocimiento de productos DIFERIDOS (ADR-0016 seccion 9): el caption se sanea y se persiste pero NO se envia a la IA; el costo por imagen (~1,3-1,6k tokens, comparable a un turno de texto) exige cerrar antes AI-QUOTA-ALERTS-1 y AI-GATE-RESERVA-1. CUATRO RONDAS de revision adversarial, todas con hallazgos reales corregidos dentro del mismo programa: el caption SI llegaba al prompt de la IA por el historial de mensajes; un adjunto degradado no producia ninguna respuesta al cliente; el bot rompia su propio silencio cuando el archivo no traia caption con el chat tomado; un comprobante marcado era invisible desde el panel de Pedidos; se prometia al cliente una revision que nadie veia; y la purga validaba el path contra el tenantId que declaraba el propio documento (primitiva de borrado cross-tenant). Se ELIMINO orders/comprobanteImage.ts, codigo muerto con el contrato retirado que un solo import habria reconectado. SUPERFICIE ESTIMADA DE RELEASE (NO desplegada): ~6 functions nuevas + el grafo de consumidores del webhook/engine/orders (~40 UPDATE), rules de Firestore y Storage, 1-2 indices y hosting.
- **AISLAMIENTO DE SECRETOS POR ENTORNO + RUNBOOK EJECUTABLE — EN REPO, SIN DEPLOY (2026-08-03, RELEASE-SECURITY-AND-RUNBOOK-HARDEN-1).** FUGA HACIA ADELANTE CORREGIDA EN DOS CAPAS INDEPENDIENTES. (1) `build-deploy.mjs` copiaba cualquier `.env.<algo>` no-`.local` porque NO SABIA a que proyecto se desplegaba —esa era la causa de fondo, no el regex—: ahora el destino es obligatorio (GCLOUD_PROJECT que firebase expone al predeploy via lifecycleHooks.js:61, ya resuelto de alias por needProjectId, o `--project` explicito) y la allowlist es `.env` + `.env.<projectId>` + `.env.<alias-que-resuelve-a-ese-projectId>` (los alias importan: env.js:149-153 tambien los carga, y excluirlos EN SILENCIO seria tan malo como incluir el de otro entorno). Aborta ante env faltante, proyecto ambiguo o env ajeno detectado al releer el artefacto. Los efectos quedaron detras de un guard de CLI: antes IMPORTARLO desde un test ejecutaba una build entera. (2) Los dos `firebase*.json` excluyen `.env*` del zip; hacia falta igual porque `firebase.json` apunta a apps/functions DIRECTO y subia los DOS entornos juntos, y la subida ocurre ANTES de que Cloud Build falle por `workspace:*` (o sea que hasta un deploy fallido filtraba). Excluirlos NO cambia ninguna variable desplegada: firebase lee los .env del disco local en prepare (loadUserEnvs) y el ignore solo gobierna el zip. PROBADO con el walker real del CLI que el repo ejecuta (13.35.1): firebase.json 2191 archivos y firebase.functions.json 874, CERO .env subidos, variables identicas (10 prod / 8 staging). SCRIPTS: los 6 `deploy:*` dejaron de traer `--project` hardcodeado (deploy:rules apuntaba a staging y devolvia EXIT 0 con produccion intacta: falso positivo perfecto); el helper falla cerrado si falta y AHORA RESUELVE ALIAS al validar —«el alias no es el destino» existia solo en la auditoria estatica, no en la ruta que ejecuta—. RUNBOOK: `hosting:rollback` NO EXISTE en NINGUNA de las dos versiones en juego (13.35.1 del repo y 15.23.0 global); el real es `hosting:clone`, que ademas tiene dos falsos positivos verificados (`--project` inerte, y un typo de canal CREA un canal preview con EXIT 0 dejando live intacto). `gcloud` NO esta instalado, asi que el freno del scheduler va por REST; se documento pausa Y reanudacion, y que borrar la funcion destruye su job. El selector del rollback es el del deploy MENOS las CREATE (si no, firebase aborta el comando entero). REVISION ADVERSARIAL: 3 NO_GO, todos correctos, el peor contra el propio test nuevo —hardcodeaba la ruta del firebase-tools GLOBAL: rompia CI (ubuntu-latest), pasaba VACUO donde no rompia (Set vacio ⇒ el assert de hosting:rollback se cumplia contra la nada) y validaba un binario que el repo no ejecuta—; ahora resuelve desde node_modules y TIRA si no lo encuentra. VERIFICACION: typecheck 0, lint 0 errores, 2.956 tests (2.198 functions + 310 shared + 448 web), build 0, deploy-guard --audit 0, git diff --check limpio. EXPOSICION: el bucket gcf-v2-sources NO es publico (UBLA, cero allUsers/allAuthenticatedUsers, 4 roles con 1-2 principals) y los 4 secretos tienen VALORES DISTINTOS en cada entorno (solo LOG_LEVEL coincide, y no es credencial) ⇒ la fuga es INTERNA y NO alcanza a produccion. ARTEFACTOS HISTORICOS PENDIENTES, NO BORRADOS: 224 versiones (115 vivas + 109 archivadas); solo 6 vivas son posteriores a la mitigacion, quedan 109 vivas construidas con el .env de staging (INFERIDO: confirmarlo exigiria descargar un zip con secretos). ROTACION TODAVIA NO EJECUTADA. PRODUCCION SIN CAMBIOS: cero deploy, cero rotacion, cero IAM, cero borrados.
- **DEFECTOS QUE DESTAPO EL DEPLOY DE ADJUNTOS (2026-08-01/03) — cada uno necesita programa propio.** (1) **`releaseToBot` DESTRUYE EL ESTADO DE CHECKOUT (ALTO, preexistente de HANDOFF-2/HUMAN-HANDOFF-1)**: el boton "devolver al asistente" del panel (conversation/handoff.ts) escribe `state:'IDLE'` incondicionalmente y limpia handoffReason, asi que borra el AWAITING_PAYMENT que puso coverageResume; a partir de ahi el receipt gate deniega con `no_explicit_payment_context` y NUNCA propone. REPRODUCIDO EN PRODUCCION: pago instruido 11:50:48 → chat liberado 11:51:11 → comprobante 11:52:03 → denegado; con el estado repuesto (el cliente escribio *pagar* otra vez) el gate propuso el candidato sin problema. Nada se pierde (el archivo queda guardado y visible, marcar a mano sigue funcionando) pero la automatizacion queda muda en el camino mas comun, porque con Coverage `required` casi todo pedido pasa por manos humanas. CONTRASTE QUE ACOTA EL ARREGLO: la liberacion AUTOMATICA de cobertura (coverageResume.liberarSesionGuardado) esta bien hecha y NO toca `state`. (2) **EL ARTEFACTO FILTRA SECRETOS DE STAGING A PRODUCCION (ALTO)**: build-deploy.mjs copia cualquier `.env.<algo>` que no termine en `.local`, asi que `.env.vpw-staging` (4 secretos, entre ellos WHATSAPP_APP_SECRET y TENANT_SECRETS_ENCRYPTION_KEY de staging) viajaba al bucket de fuentes de PROD. En este release se mitigo moviendo el archivo fuera durante el deploy (verificado: el artefacto solo llevo .env.vpw-prod-dd6ff), pero deploys ANTERIORES ya lo subieron: **conviene ROTAR esos 4 secretos de staging**. Arreglo de fondo: acotar el filtro a `.env` + `.env.<projectId>`. (3) **`hosting:rollback` NO EXISTE** en firebase-tools 15.23.0 — el runbook documentaba un comando invalido justo en el paso 1 del rollback; el real es `hosting:clone <site>@<versionId> <site>:live`. (4) **`pnpm deploy:rules` esta hardcodeado a vpw-staging** desde 3939676: usarlo creyendo que despliega prod deja produccion intacta Y sale con exito (falso positivo perfecto). (5) **El rollback no frena schedulers**: hay que pausarlos aparte con `gcloud scheduler jobs pause`. (6) Cuatro secretos productivos siguen como env vars planas en vez de Secret Manager, contra la politica escrita del propio repo. Los seis quedaron documentados en docs/deploy.md y docs/HANDOFF.md.
- **ENDURECIMIENTO DE ROLLOUT DE ADJUNTOS — DESPLEGADO EN `30c1687` (2026-07-31 en repo, 2026-08-01/03 en produccion; ADR-0016 secciones 10/11/12/13, WHATSAPP-MEDIA-SAFE-RELEASE-HARDEN-1)**. Cierra los tres bloqueantes que impedian desplegar la fundacion de medios. (1) NO EXISTIA NINGUN ROLLOUT: desplegar encendia la fundacion sola, para todos los tenants a la vez. Ahora hay dos interruptores independientes, por tenant y fail-closed: config/attachments.ingest.enabled (guardar archivos) y config/receiptGate.enabled (proponer comprobantes). SOLO el booleano exacto true enciende: ausente, false, "true", 1, "1", [true] y cualquier otra forma son OFF, y un error de Firestore al leer el interruptor tambien apaga (al reves que los LIMITES, que si caen a defaults para no perder el archivo del cliente). El flag del nivel B se RELEE DENTRO de la transaccion, asi un apagado ya commiteado le gana a cualquier lectura previa. Nivel A en OFF: cero Graph, cero Storage, cero documento de adjunto, pero el inbound queda visible con mensaje neutral y respuesta honesta, sin caption a la IA, sin tocar pedidos y sin handoff; NO se restaura el camino legacy. Nivel B en OFF: el archivo se guarda y queda generic_media, attachmentMarkAsReceipt RECHAZA y attachmentUnmarkReceipt SIGUE disponible (apagar una funcion no puede dejar atrapada una decision humana); nada ya guardado se oculta ni se borra. Los flags no son escribibles desde ningun cliente (rules cierran config/{doc} con write:false y ninguna callable los toca). (2) CARRERA TAKEOVER-ENVIO: la regla de silencio de HANDOFF-2 vivia triplicada (motor, un re-chequeo parcial que ignoraba botEnabled, y una version ad-hoc rama por rama en ubicacion nativa); ahora vive en conversation/silencio.ts y se relee autoritativamente pegada al POST, fail-closed; una respuesta suprimida NO se persiste; el desenlace tipado del transporte decide que queda en el historial (rejected no se registra, unknown si porque pudo salir). Alcance honesto: cubre el webhook entrante (motor, adjuntos, ubicacion), NO la entrega por outbox de la reanudacion de cobertura, que la dispara una aprobacion humana. (3) PROMESA SIN SENAL: candidato y campana se confirman en la MISMA transaccion con id determinista, o ninguno; si no commitea no se promete revision, el pedido no se toca y la respuesta es neutral. LIMITACION CONOCIDA declarada en el ADR: la campana no lleva targetUid, asi que la ven OWNER y MANAGER pero no el rol SELLER (en el camino de un adjunto entrante no hay vendedor asignado a quien dirigirla, y ampliar la rule expondria avisos de otros clientes). REVISION ADVERSARIAL (5 revisores frescos + 6 auditores previos, ningun NO-GO) encontro UNA REGRESION REAL introducida por el propio fix: el guard de silencio se habia metido ENTRE el kill-switch de cobertura y el sendText, ensanchando con dos lecturas una ventana que era de cero E/S por diseno; el kill-switch volvio a ser el ultimo gate (gateFinal) con test que lo fija. El orquestador encontro ademas que el chequeo de contrato entre motivos del gate y respuestas al cliente NO SE EJECUTABA en ningun lado (tsconfig excluye los .test.ts y vitest no verifica tipos) y ya habia dejado pasar un motivo sin mensaje: ahora recorre un arreglo real en runtime. SELECTOR EXACTO DEL RELEASE (escrito porque el rollback lo exige): CREATE = attachmentGetViewUrl, attachmentMarkAsReceipt, attachmentUnmarkReceipt, attachmentRetentionMaintenance; UPDATE = onWebhookInbox, metaWebhook; infra = Firestore Rules, Storage Rules, Hosting; CERO indices compuestos nuevos. Orden de deploy: rules, consumidor (onWebhookInbox) primero, los 3 callables, productor (metaWebhook) ultimo, scheduler, hosting. ORDEN DE ROLLBACK = EL INVERSO (hosting, metaWebhook, onWebhookInbox) y SIN las 4 CREATE: en el codigo anterior esos exports no existen y firebase deploy aborta el comando entero. El deploy con los flags apagados NO es inerte: los flags gobiernan guardar bytes y proponer comprobantes, no restauran el camino legacy que 12a899a elimino. UN DEFECTO MAS que destapo el propio hardening, en cobertura: la respuesta honesta "no puedo procesarla" —la que existe PORQUE el kill-switch se apago— quedaba etiquetada con el activationId y por lo tanto la frenaba el mismo interruptor que la provocaba, asi que el cliente mandaba su ubicacion y recibia silencio total; no se notaba porque la burbuja se persistia igual y el panel mostraba una respuesta que el cliente nunca recibio. Ahora solo se etiqueta la respuesta que CONFIRMA o PROMETE revision. VERIFICACION: typecheck 0, lint 0 errores (5 warnings preexistentes), build 0, git diff --check 0, 2.924 tests (2.166 functions + 310 shared + 448 web), y E2E 310 checks todos en verde desde emulador limpio y serializado: verify-attachments 98/98 dos veces (incluye el bloque nuevo AT-23, que prueba el estado real del dia del deploy con los dos flags apagados), handoff2 8/8, human-handoff 11/11, order-comprobante 9/9, fase4-whatsapp 9/9, p6-rules 5/5, rules-config 12/12, coverage-killswitch 23/23, coverage-state 29/29, coverage-guard 8/8.
- Último commit desplegado conocido: **`30c1687`** (2026-08-01/03: ADJUNTOS DE CONVERSACION desplegados y VALIDADOS en arfagi, ADR-0016 — 4 functions create (attachmentGetViewUrl, attachmentMarkAsReceipt, attachmentUnmarkReceipt, attachmentRetentionMaintenance) + 2 update (onWebhookInbox 256Mi/60s→512Mi/180s, metaWebhook) para un total de **115 ACTIVE**, Firestore Rules + Storage Rules + Hosting, **CERO indices nuevos** (siguen 18/18 READY), schedulers 6→7 con invoker restringido al SA. Orden: rules → consumidor → callables → productor → scheduler → hosting; rollback al reves y SIN las 4 CREATE. Rollout en dos niveles fail-closed activo SOLO en arfagi (`config/attachments.ingest.enabled=true` + `config/receiptGate.enabled=true`, booleanos exactos, con precondicion fresca y updateMask exclusivo); **credipower con ambos documentos AUSENTES y cero adjuntos**; **purga APAGADA**. Tres smokes humanos en produccion: flags OFF (16/16), medios genericos con MIME verificado y silencio real durante takeover, y cadena completa candidato→marca humana→PENDING_VERIFICATION→desmarcado→PENDING_PAYMENT **sin pasar nunca por PAID**, con las dos auditorias conservadas (`paymentConfirmed=false`) y el archivo intacto en Storage. Pedido de prueba CANCELLED sin pago; **cero pagos y cero PAID nuevos en todo el proyecto**. Verificado que NINGUN turno de adjunto llamo a la IA. Limitacion conocida documentada: la campana no lleva `targetUid`, asi que un SELLER puro no la ve — no se ampliaron las Rules). Anterior: `d542cda` (2026-07-31: modelo de propiedad por campos del catálogo, ADR-0015 — 6 functions create + 39 update para un total de 111 ACTIVE, 0 índices, 3 rules cerradas, hosting; arfagi migrado a `external_managed` con cero campos escribibles y `mode` en `dry_run`; primera reconciliación con 50 verified / 131 drifted_external; smoke humano aprobado). Anterior: `6cfc464` (2026-07-29: onboarding genérico de catálogos + centro de calidad, en dry-run — perfil `perfumeria` explícito de arfagi, 1 índice compuesto nuevo, 3 rules cerradas + campana de calidad para manager, 5 functions create + 21 update, hosting; mantenimiento apply: 30 locks creados + 1 preservado = 31 totales, 32 quality, 0 conflictos; smoke aprobado). Anterior: `fbb7aab` (2026-07-28: preview binding del catálogo — 3 functions update + hosting, cero create/delete/rules/índices; smoke aprobado). Anterior: `50793bc` (2026-07-28: outbox de catálogo Meta completo, dry-run inerte — 2 índices create, rules, 4 functions create + 11 update, hosting; smoke aprobado). Anterior: `141c4ab` (2026-07-27: reconciliación de catálogo Meta — 17 functions update / 4 create, cero Rules/índices; mapping de Odyssey y 30 artículos importados INACTIVE). Anterior: `eb28365` (2026-07-25: PURGE-FIX-1 — purga de coordenadas en decisiones terminales; 4 callables update / 0 create / 0 delete; backfill de 2 requests históricos; Coverage REACTIVADO solo en arfagi con activationId nuevo, required + maxChargeGs ₲200.000; credipower apagado).
- Último smoke real de catálogo: 2026-07-31 (release del modelo de propiedad y migración de arfagi a external_managed, ver arriba). Anterior: 2026-07-30 (importación genérica). Último smoke de venta end-to-end (2026-07-25, E2E COVERAGE/SHIPPING-CHAT COMPLETO con tester interno): checkout gateado → ubicación → rechazo → reintento → cotización ₲30.000 → canónico único → aprobación → pedido único ₲280.000 → banco una vez → comprobante → cancelación manual sin pago. Cero IA, cero PAID, privacidad verificada. FASE 3 con número externo sigue PENDIENTE.

### Próxima fase prioritaria

**0. CORREGIR EL PRECIO DE ODYSSEY EN EL ORIGEN QUE GENERA EL FEED (PRIORIDAD MÁXIMA, fuera de este repo)**: el modelo de propiedad ya está desplegado (`d542cda`, 2026-07-31) y arfagi ya está en `external_managed`, así que el sistema detecta la divergencia, la muestra en el panel y NO cierra venta automática de Odyssey. Falta el paso que este proyecto no puede hacer solo: que el sistema del sitio de arfagi que GENERA el CSV del feed publique ₲250.000 en vez de ₲130.000. Tras la publicación diaria siguiente (03:23-03:24 America/Asuncion), la reconciliación de las 04:30 debe devolver Odyssey de `drifted_external` a `verified` y el guard debe dejar de bloquearlo — esa es la prueba final de que el modelo funciona. El feed no se toca, no se ejecuta a mano y no se desactiva; no se escribe en Meta; `mode` sigue en `dry_run`. Deuda menor del release (no bloquea la venta): la tarjeta de propiedad muestra la fuente sin nombre ni horario, y `metaSyncState` no se recalcula al editar un producto, así que al activar un importado enriquecido arrastra el `drifted_external` por disponibilidad hasta la reconciliación siguiente (máximo ~12 h).

1. Evidencia de Fase 3 (Meta en vivo): prueba completa con un número EXTERNO (no del owner) — inbound real → respuesta del bot → carrito → orden → comprobante visible en el panel → logs sin errores. Verificación read-only. Sigue PENDIENTE: todas las pruebas hasta ahora salieron de números del owner.
2. Fase 5 — Operación: runbook para el owner, backups semanales documentados, alertas mínimas (presupuesto GCP + errores de functions) y documentación operativa al día.
3. Programas pendientes ya identificados (AI-FALLBACK-HONESTO-1, COVERAGE-GUARD-1 y el paquete COVERAGE con flag OFF ya DESPLEGADOS): COVERAGE — infraestructura EN PRODUCCIÓN con el flag APAGADO (commit `0326784`); Coverage + Shipping Chat: E2E real VALIDADO y **Coverage ACTIVO solo en arfagi** (2026-07-25, `required` + max ₲200.000; purga de coordenadas corregida con backfill). Próximo: evidencia FASE 3 con número EXTERNO: tester sin pendingOrderId, cero jobs/sesiones no-terminales, ventana de bajo tráfico, seller configurado, activationId nuevo, rollback, y smoke completo rechazo → nueva ubicación → aprobación → retorno del bot. NO se declara Coverage funcionalmente completo hasta esa activación + E2E real. El hallazgo del guard que la máquina de estados cubre: "¿el costo de envío se calcula antes o después de pagar mi pedido?" clasificado como vista de carrito por contener "mi pedido" (honesto, sin efectos, UX incorrecta). AI-QUOTA-ALERTS-1 (alertas 70/85/95/100%), AI-PROMPT-CACHING-1, microprograma de cortesía determinística ("Gracias" consumió un turno de IA de ~3.4k tokens innecesariamente), higiene de logs (customerId completo — el teléfono — en metadata estructurada de logs; preexistente, enmascarar), AI-USAGE-ATTRIBUTION-1, AI-GATE-RESERVA-1, Meta Catalog Live, campana para rol SELLER puro (hoy sin impacto) y microajuste de comparaciones contextuales ("¿ambas son dulces?").
4. Criterio de cierre del proyecto: una persona externa completa el ciclo entero sin intervención técnica (WhatsApp → recomendación → carrito → orden → comprobante → el vendedor lo ve y confirma en el panel desde vendeyapy.com).

Después (backlog, solo a pedido del owner):

1. `CATALOG-IMPORT-1`: importación desde CSV/Excel/Sheets.
2. `META-CATALOG-SYNC-1`: sincronización con Meta Commerce/WhatsApp.
3. `CATALOG-ENRICHMENT-3`: generación asistida de fichas IA.
4. `WHATSAPP-COEXISTENCE-1`: Embedded Signup con coexistencia.
5. `META-ADS-GROWTH`: métricas, anuncios, ROAS y recomendaciones.
6. Integración genérica con CRM, postergada.

## Forma de trabajo

Cada fase sigue este formato:

1. Codex analiza el estado, riesgos y dependencias.
2. Codex redacta un prompt técnico completo para Claude Code.
3. Claude Code audita el repositorio antes de modificar.
4. Implementa el cambio más pequeño compatible con la arquitectura existente.
5. Ejecuta typecheck, lint, tests, E2E y regresiones.
6. Realiza review adversarial cuando corresponde.
7. Commita y pushea, pero no despliega sin aprobación explícita.
8. El usuario pega en Codex el reporte completo de Claude Code.
9. Codex revisa resultados, detecta vacíos y prepara el deploy o la siguiente fase.
10. Los despliegues se verifican con smoke técnico y, cuando corresponde, una prueba real controlada.

Los reportes deben incluir:

- Qué se auditó.
- Causa raíz.
- Cambios realizados.
- Archivos o módulos afectados.
- Tests y regresiones.
- Commit.
- Estado de deploy.
- Riesgos o pendientes.
- Próximo prompt recomendado.

## Relación Codex–Claude Code

Codex y Claude Code no comparten automáticamente sus conversaciones.

- **Codex:** mantiene la visión global, revisa resultados, define fases, seguridad, alcance y prompts.
- **Claude Code:** trabaja directamente sobre el repositorio, implementa, prueba, commitea y despliega cuando se autoriza.
- **Usuario:** actúa como puente, copiando el prompt de Codex a Claude Code y pegando su reporte nuevamente en Codex.

Codex también puede trabajar directamente en el repositorio si está disponible localmente; Claude Code no es obligatorio. El flujo actual lo utiliza como ejecutor principal y a Codex como coordinador técnico y revisor.

## Reglas esenciales

- Leer `C:\AI_AFG\AGENTS.md` antes de trabajar.
- No tocar `_archive/` ni `80-creditos.future/`.
- No modificar producción ni desplegar sin aprobación explícita.
- No imprimir ni compartir secretos, tokens, claves `.env` o credenciales por chat/WhatsApp.
- `.env` no se commitea.
- Mantener aislamiento multi-tenant.
- No hardcodear Arfagi/perfumería en backend.
- No hacer hard-delete de pedidos.
- Ejecutar como mínimo `pnpm -r typecheck` y tests relacionados.
- Conservar cambios existentes del usuario.
- Functions, hosting, Firestore Rules y Storage Rules se despliegan por separado según la fase.

En la computadora nueva: cloná el repositorio, iniciá Codex desde la raíz, asegurate de que lea `AGENTS.md` y pegale este resumen. Los secretos deben recuperarse mediante Firebase Secret Manager o un canal seguro, nunca desde Git ni WhatsApp.
### Coexistence — 2026-08-04

Fundación de WhatsApp Business App Coexistence COMPLETA EN REPO y NO DESPLEGADA. Número real no conectado,
QR no ejecutado, backup productivo no ejecutado (se construyeron y probaron las herramientas).

`automationMode: inactive|shadow|live` separa "la credencial sirve" de "el bot puede contestar", fail-closed.
Embedded Signup en dos flujos; el de Coexistence usa `featureType = whatsapp_business_app_onboarding` y crea
una conexión `wa_{pnid}` que jamás toca `metaConnections/main`. El PNID se resuelve server-side desde el WABA
porque el evento de cierre oficial no lo trae. El historial hay que PEDIRLO (`smb_app_data`) y se pide UNA
sola vez: repetirlo exige que el cliente offboardee y rehaga el flujo.

El cutover es el Programa 2: `docs/coexistence-release-runbook.md`. La migración del PNID actual a `live` va
ANTES del deploy. Gate externo abierto: elegibilidad de Paraguay, que Meta no documenta.

### Correctivo Coexistence — 2026-08-04 (cierre del Programa 1)

Cerrados los 7 gates restantes: live real por herramienta (gate transicional retirado con garantía
estructural + E2E dual 20/20), IG/Messenger con canal propio, cutover transaccional con rollback byte
a byte (verify-cutover 22/22), UI humana del historial (share/skip/disparo/estado), generaciones del
historial (offboard cierra honesto; solo un signup nuevo abre la siguiente), backup de Storage con
bytes + restore aislado (backup-restore-e2e 51/51, exige los tres emuladores). Sigue: NO desplegado,
número real NO conectado, QR NO ejecutado, backup productivo NO ejecutado. Cutover = Programa 2.

### Cierre correctivo final — 2026-08-05
11 hallazgos de Codex Security + 8 de la review adversarial posterior, cerrados con test rojo
primero. El CRITICAL fue del propio correctivo anterior: un mapa vacío con merge REEMPLAZA en
Firestore, y el guard monotónico borraba el resumen del cliente. Dos pasadas E2E completas desde
emuladores limpios (14 suites cada una). Sin deploy, sin Meta, sin producción. Cuatro pruebas
externas quedan diferidas al Programa 2 (contrato de timestamp, cardinalidad de WABA, prueba de
offboarding, provenance de bundles) — resueltas fail-closed en código, sin validación real.

### Programa 2 — Preflight del release (2026-08-06)
Etapa A verde completa (batería + E2E + grafo 122 exports, CREATE=6/UPDATE=115/DELETE=0) y baseline
productiva congelada read-only (un solo PNID, automationMode ausente = migracion_pendiente esperado;
credipower intacto). Runbook corregido con 6 precisiones doc-only (lista real de claves de Hosting,
selector de reversa sin los 6 CREATE, prefijo GCLOUD_PROJECT obligatorio en migrate, statuses dentro
de messages, CLI del repo, comentario TTL). Detenido fail-closed antes de toda mutación: 5 gates
externos de Meta sin evidencia (incl. Paraguay), config_id de Coexistence sin crear, ADC ausente en
la máquina (backups/migración/kill-switch inejecutables), y decisión v2/v3 pendiente del owner.
